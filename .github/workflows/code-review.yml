name: Code Review (OpenCodeReview)

# Reusable, org-wide AI code review powered by OpenCodeReview (OCR).
# Keyword guard + concurrency are centralized here; callers only declare `on:`.
# Callers MUST grant `permissions: pull-requests: write` (the org default token is
# read-only and a reusable workflow cannot exceed the caller's permissions).
# Project rules are auto-loaded from <repo>/.opencodereview/rule.json.
#
# Required org/repo secrets (passed via `secrets: inherit`):
#   OCR_LLM_URL, OCR_LLM_AUTH_TOKEN
# Optional:
#   OCR_LLM_MODEL (default glm-5.2), OCR_LLM_USE_ANTHROPIC (default true)
#   GH_APP_CLIENT_ID (or legacy GH_APP_ID) + GH_APP_MUNMIU_PRIVATE_KEY -> post comments as munmiu[bot]
#     (falls back to github-actions[bot] when absent)

on:
  workflow_call:
    secrets:
      OCR_LLM_URL:
        required: true
      OCR_LLM_AUTH_TOKEN:
        required: true
      OCR_LLM_MODEL:
        required: false
      OCR_LLM_USE_ANTHROPIC:
        required: false
      # Optional: post review comments as the munmiu GitHub App instead of
      # github-actions[bot]. If absent/empty, falls back to GITHUB_TOKEN.
      GH_APP_ID:
        required: false
      GH_APP_CLIENT_ID:
        required: false
      GH_APP_MUNMIU_PRIVATE_KEY:
        required: false

# Centralized here so callers stay minimal — change the trigger keyword in one place.
# cancel-in-progress: false so an unrelated PR comment (e.g. another bot) does not
# cancel an in-flight review — the comment run queues, then skips via the job `if`.
concurrency:
  group: code-review-${{ github.event.pull_request.number || github.event.issue.number || github.ref }}
  cancel-in-progress: false

permissions:
  contents: read
  pull-requests: write

jobs:
  code-review:
    # Auto-review on PR open/reopen, EXCEPT bot-authored PRs (release-please,
    # dependabot, etc.) — review those on demand only. A `/ocr` (or `@ocr`) PR
    # comment always forces a review, including on bot PRs.
    if: >
      (github.event_name == 'pull_request_target' &&
        github.event.pull_request.user.type != 'Bot') ||
      (github.event_name == 'issue_comment' && github.event.issue.pull_request != null &&
        (startsWith(github.event.comment.body, '/ocr') ||
         startsWith(github.event.comment.body, '@ocr')))
    runs-on: ubuntu-latest
    steps:
      - name: Get PR context
        id: pr-context
        if: github.event_name != 'pull_request_target'
        uses: actions/github-script@v9
        with:
          script: |
            const prNumber = context.issue.number;
            const { data: pr } = await github.rest.pulls.get({
              owner: context.repo.owner,
              repo: context.repo.repo,
              pull_number: prNumber,
            });
            core.setOutput('base_ref', pr.base.ref);
            core.setOutput('head_ref', pr.head.ref);
            core.setOutput('head_sha', pr.head.sha);

      - name: Checkout repository
        uses: actions/checkout@v6
        with:
          fetch-depth: 0 # full history needed for merge-base diff
          ref: ${{ github.event.pull_request.head.sha || steps.pr-context.outputs.head_sha }}

      - name: Fetch PR head ref (ensures fork commits are available)
        run: git fetch origin pull/${{ github.event.pull_request.number || github.event.issue.number }}/head

      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version: '22'

      - name: Install OpenCodeReview
        run: npm install -g @alibaba-group/open-code-review

      - name: Configure OCR
        env:
          OCR_LLM_URL: ${{ secrets.OCR_LLM_URL }}
          OCR_LLM_AUTH_TOKEN: ${{ secrets.OCR_LLM_AUTH_TOKEN }}
          OCR_LLM_MODEL: ${{ secrets.OCR_LLM_MODEL }}
          OCR_LLM_USE_ANTHROPIC: ${{ secrets.OCR_LLM_USE_ANTHROPIC }}
        run: |
          ocr config set llm.url "$OCR_LLM_URL"
          ocr config set llm.auth_token "$OCR_LLM_AUTH_TOKEN"
          ocr config set llm.model "${OCR_LLM_MODEL:-glm-5.2}"
          ocr config set llm.use_anthropic "${OCR_LLM_USE_ANTHROPIC:-true}"
          ocr config set llm.extra_body '{"thinking": {"type": "disabled"}}'

      - name: Run OpenCodeReview
        id: review
        env:
          EVENT_NAME: ${{ github.event_name }}
          PR_BASE_REF: ${{ github.event.pull_request.base.ref }}
          PR_HEAD_SHA: ${{ github.event.pull_request.head.sha }}
          CTX_BASE_REF: ${{ steps.pr-context.outputs.base_ref }}
          CTX_HEAD_SHA: ${{ steps.pr-context.outputs.head_sha }}
          PR_TITLE: ${{ github.event.pull_request.title }}
        run: |
          if [ "$EVENT_NAME" = "pull_request_target" ]; then
            BASE_REF="$PR_BASE_REF"; HEAD_SHA="$PR_HEAD_SHA"
          else
            BASE_REF="$CTX_BASE_REF"; HEAD_SHA="$CTX_HEAD_SHA"
          fi
          echo "Reviewing ${HEAD_SHA} against origin/${BASE_REF}"

          # HEAD_SHA (not origin/<head_ref>) so fork PRs work; rule.json auto-loads from .opencodereview/.
          ARGS=(review --from "origin/${BASE_REF}" --to "${HEAD_SHA}" --format json)
          if [ -n "${PR_TITLE}" ]; then ARGS+=(--background "${PR_TITLE}"); fi
          ocr "${ARGS[@]}" > /tmp/ocr-result.json 2>/tmp/ocr-stderr.log || true

          echo "OCR output:"; cat /tmp/ocr-result.json
          echo "OCR stderr:"; cat /tmp/ocr-stderr.log

      - name: Get munmiu app token
        id: app-token
        continue-on-error: true # absent app secrets / not installed → fall back to GITHUB_TOKEN
        uses: actions/create-github-app-token@v3
        with:
          client-id: ${{ secrets.GH_APP_CLIENT_ID || secrets.GH_APP_ID }}
          private-key: ${{ secrets.GH_APP_MUNMIU_PRIVATE_KEY }}

      - name: Post review comments to PR
        uses: actions/github-script@v9
        with:
          github-token: ${{ steps.app-token.outputs.token || secrets.GITHUB_TOKEN }}
          script: |
            const fs = require('fs');
            const path = '/tmp/ocr-result.json';

            let result;
            try {
              result = JSON.parse(fs.readFileSync(path, 'utf8'));
            } catch (e) {
              console.log('Failed to parse OCR output:', e.message);
              const stderr = fs.readFileSync('/tmp/ocr-stderr.log', 'utf8').trim();
              if (stderr) {
                await github.rest.issues.createComment({
                  owner: context.repo.owner,
                  repo: context.repo.repo,
                  issue_number: context.issue.number,
                  body: `⚠️ **OpenCodeReview** encountered an error:\n${fencedBlock(stderr)}`,
                });
              }
              return;
            }

            const warnings = result.warnings || [];

            // Honor `exclude` globs from <repo>/.opencodereview/rule.json: drop
            // comments on vendored/tracked paths so they don't spam the PR. OCR
            // has no native path exclusion, so we post-filter its output here.
            let excludeGlobs = [];
            try {
              const ws = process.env.GITHUB_WORKSPACE || '.';
              const rj = JSON.parse(fs.readFileSync(`${ws}/.opencodereview/rule.json`, 'utf8'));
              if (Array.isArray(rj.exclude)) excludeGlobs = rj.exclude.filter((g) => typeof g === 'string');
            } catch (e) { /* no rule.json / no exclude — review everything */ }
            const excludeRes = excludeGlobs.map(globToRegExp);
            const isExcluded = (p) => !!p && excludeRes.some((re) => re.test(p));
            const rawComments = result.comments || [];
            const comments = rawComments.filter((c) => !isExcluded(c.path));
            const excludedCount = rawComments.length - comments.length;
            if (excludedCount > 0) {
              console.log(`Excluded ${excludedCount}/${rawComments.length} comment(s) matching .opencodereview/rule.json exclude globs: ${excludeGlobs.join(', ')}`);
            }

            if (comments.length === 0) {
              if (/no supported files changed/i.test(result.message || '')) {
                console.log(`OpenCodeReview skipped PR comment: ${result.message}`);
                return;
              }
              const message = result.message || 'Review completed with no findings.';
              await github.rest.issues.createComment({
                owner: context.repo.owner,
                repo: context.repo.repo,
                issue_number: context.issue.number,
                body: `✅ **OpenCodeReview**: ${message}`,
              });
              return;
            }

            const prNumber = context.issue.number;
            let commitSha;
            if (context.eventName === 'pull_request_target') {
              commitSha = context.payload.pull_request.head.sha;
            } else {
              const { data: pr } = await github.rest.pulls.get({
                owner: context.repo.owner,
                repo: context.repo.repo,
                pull_number: prNumber,
              });
              commitSha = pr.head.sha;
            }

            const reviewComments = [];
            const commentsWithoutLine = [];
            for (const comment of comments) {
              const body = formatComment(comment);
              const hasValidLine = (comment.start_line >= 1) || (comment.end_line >= 1);
              if (!hasValidLine) {
                commentsWithoutLine.push({ comment, body });
                continue;
              }
              const reviewComment = { path: comment.path, body };
              if (comment.start_line >= 1 && comment.end_line >= 1 && comment.start_line !== comment.end_line) {
                reviewComment.start_line = comment.start_line;
                reviewComment.line = comment.end_line;
                reviewComment.start_side = 'RIGHT';
                reviewComment.side = 'RIGHT';
              } else if (comment.end_line >= 1) {
                reviewComment.line = comment.end_line;
                reviewComment.side = 'RIGHT';
              } else if (comment.start_line >= 1) {
                reviewComment.line = comment.start_line;
                reviewComment.side = 'RIGHT';
              }
              reviewComments.push({ comment, reviewComment });
            }

            const totalCount = comments.length;
            const inlineCount = reviewComments.length;
            const summaryCount = commentsWithoutLine.length;
            let summaryBody = buildSummaryBody(totalCount, inlineCount, summaryCount, warnings);
            summaryBody += formatSummaryComments(commentsWithoutLine);

            let successCount = 0;
            let failedCount = 0;
            const failedComments = [];

            try {
              await github.rest.pulls.createReview({
                owner: context.repo.owner,
                repo: context.repo.repo,
                pull_number: prNumber,
                commit_id: commitSha,
                body: summaryBody,
                event: 'COMMENT',
                comments: reviewComments.map(({ reviewComment }) => reviewComment),
              });
              successCount = reviewComments.length;
              console.log(`Posted review with ${successCount} inline comments (${commentsWithoutLine.length} in summary)`);
            } catch (e) {
              console.log('Failed to post review with inline comments:', e.message);
              console.log('Falling back to posting comments individually...');
              for (const { comment, reviewComment } of reviewComments) {
                try {
                  await github.rest.pulls.createReview({
                    owner: context.repo.owner,
                    repo: context.repo.repo,
                    pull_number: prNumber,
                    commit_id: commitSha,
                    body: '',
                    event: 'COMMENT',
                    comments: [reviewComment],
                  });
                  successCount++;
                } catch (innerE) {
                  failedCount++;
                  failedComments.push({ comment, error: innerE.message });
                }
              }
              let finalBody = buildSummaryBody(totalCount, successCount, commentsWithoutLine.length + failedComments.length, warnings);
              finalBody += formatSummaryComments(commentsWithoutLine);
              finalBody += `\n\n---\n\n📊 **Posting Statistics:**`;
              finalBody += `\n- ✅ Successfully posted: ${successCount} comment(s)`;
              if (failedCount > 0) {
                finalBody += `\n- ❌ Failed to post: ${failedCount} comment(s)`;
              }
              if (failedComments.length > 0) {
                finalBody += '\n\n---\n\n### ⚠️ Inline comments shown in summary';
                for (const { comment, error } of failedComments) {
                  finalBody += '\n\n---\n\n';
                  finalBody += formatCommentMarkdown(comment, error);
                }
              }
              await github.rest.issues.createComment({
                owner: context.repo.owner,
                repo: context.repo.repo,
                issue_number: prNumber,
                body: finalBody,
              });
            }

            // Minimal glob -> RegExp: supports ** (any, incl. /), * (non-/), ? (one non-/).
            function globToRegExp(glob) {
              const re = glob
                .replace(/[.+^${}()|[\]\\]/g, '\\$&')
                .replace(/\*\*/g, '\u0000')
                .replace(/\*/g, '[^/]*')
                .replace(/\u0000/g, '.*')
                .replace(/\?/g, '[^/]');
              return new RegExp('^' + re + '$');
            }

            function formatComment(comment) {
              let body = comment.content || '';
              if (comment.suggestion_code && comment.existing_code) {
                body += '\n\n**Suggestion:**\n';
                body += fencedBlock(comment.suggestion_code, 'suggestion');
              }
              return body;
            }

            function formatCommentMarkdown(comment, error) {
              let md = `### 📄 \`${comment.path}\``;
              if (comment.start_line && comment.end_line) {
                md += ` (L${comment.start_line}-L${comment.end_line})`;
              }
              md += '\n\n';
              if (error) {
                md += `⚠️ GitHub could not post this as an inline comment: ${error}\n\n`;
              }
              md += comment.content || '';
              if (comment.suggestion_code && comment.existing_code) {
                md += '\n\n<details><summary>💡 Suggested Change</summary>\n\n';
                md += '**Before:**\n' + fencedBlock(comment.existing_code) + '\n\n';
                md += '**After:**\n' + fencedBlock(comment.suggestion_code) + '\n\n';
                md += '</details>';
              }
              return md;
            }

            function buildSummaryBody(totalCount, inlineCount, summaryCount, warnings) {
              let body = `🔍 **OpenCodeReview** found **${totalCount}** issue(s) in this PR.`;
              if (totalCount > 0) {
                body += `\n- ✅ ${inlineCount} posted as inline comment(s)`;
                body += `\n- 📝 ${summaryCount} posted as summary`;
              }
              if (warnings.length > 0) {
                body += `\n\n⚠️ ${warnings.length} warning(s) occurred during review.`;
              }
              return body;
            }

            function formatSummaryComments(summaryComments) {
              let body = '';
              for (const { comment } of summaryComments) {
                body += '\n\n---\n\n';
                body += formatCommentMarkdown(comment);
              }
              return body;
            }

            function fencedBlock(content, language = '') {
              const text = String(content || '');
              const fence = safeFence(text);
              let block = fence + language + '\n' + text;
              if (!text.endsWith('\n')) block += '\n';
              return block + fence;
            }

            function safeFence(content) {
              const matches = String(content || '').match(/`+/g) || [];
              const maxTicks = matches.reduce((max, ticks) => Math.max(max, ticks.length), 0);
              return '`'.repeat(Math.max(3, maxTicks + 1));
            }
